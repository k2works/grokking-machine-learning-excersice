# #11 大学院入学審査の決定木（Python）

原著ノートブック: `Chapter_09_Decision_Trees/University_Admissions.ipynb`

入学審査データ 400 件から合否を決定木で当てます。
[#10](nb10.md) が 12 点の作り物だったのに対し、こちらは実データです。

**原著の構成は「同じデータで木の大きさを変えて過学習を見せる」** というもの。
制限なしの木は訓練データを 100% 当てますが、それは嬉しいことではありません。

## サンプル実装

| 項目 | 内容 |
| :--- | :--- |
| 実装 | `apps/grokking-ml-python-lib/src/grokking_ml_lib/nb11_university_admissions.py` |
| テスト | `apps/grokking-ml-python-lib/tests/test_nb11_university_admissions.py` |
| 実行 | `uv run pytest tests/test_nb11_university_admissions.py` |

## ラベルを閾値で作る

```python
def load_data() -> pd.DataFrame:
    """入学審査データを読み込み、合否のラベルを付ける。

    原著は `Chance of Admit`（合格確率）を 0.75 で切って 2 値にし、
    元の列を落としている。`index_col=0` で `Serial No.` を索引にする。
    """
    data = pd.read_csv(dataset_path("Admission_Predict.csv"), index_col=0)
    data["Admitted"] = data["Chance of Admit"] >= ADMISSION_THRESHOLD
    return data.drop(["Chance of Admit"], axis=1)
```

元データは **回帰問題**（合格確率）ですが、0.75 で切って分類問題に変えています。
`Chance of Admit` を残したままだと、それだけで完全に予測できてしまうので落とします。

```python
def test_合格ラベルは合格確率0_75で切る(data) -> None:
    # 原著は Chance of Admit >= 0.75 を合格とする
    assert ADMISSION_THRESHOLD == 0.75
    # 400 件中 180 件が合格。おおむね 45%
    assert int(labels(data).sum()) == 180
    # 元の列は落とされている
    assert "Chance of Admit" not in data.columns
```

## 制限なしの木は完全に覚える

```python
def test_制限なしの木は訓練データを完全に覚える(data, full) -> None:
    # 原著の出力: dt.score(features, labels) -> 1.0
    assert full.score(features(data), labels(data)) == 1.0
```

400 件すべてを当てます。節の数は 115 前後です。
**1 件あたり 0.29 節** — ほぼ丸暗記しているのと変わりません。

## 3 つの制限を同時に掛ける

```python
def fit_smaller(data: pd.DataFrame, random_state: int | None = 0) -> DecisionTreeClassifier:
    """原著が「過学習しない小さい木」として作る設定。

    3 つの制限を同時に掛けている。
    - `max_depth=3` — 木の深さ
    - `min_samples_leaf=10` — 葉に残る件数の下限
    - `min_samples_split=10` — 分割してよい節の件数の下限
    """
    return DecisionTreeClassifier(
        max_depth=3,
        min_samples_leaf=10,
        min_samples_split=10,
        random_state=random_state,
    ).fit(features(data), labels(data))
```

正解率は **0.885** に落ちますが、節は 15 個です。

```python
def test_小さい木は制限のぶんだけ節が減る(data, full, smaller) -> None:
    # 制限なしの木は 115 節前後まで育つ。小さい木は 15 節で収まる
    assert full.tree_.node_count > 100
    assert smaller.tree_.node_count == 15
```

**節を 8 分の 1 にして、正解率は 11.5 ポイントしか落ちません。**
訓練データの正解率だけを見ていると気づけない話です。

制限が効くと、木が決定的になる副作用もあります。

```python
def test_小さい木は種によらず同じ構造になる(data) -> None:
    # 制限が効いて同点の分割が起きにくくなり、木が決定的になる
    structures = {
        tuple(split_conditions(fit_smaller(data, random_state=seed), list(features(data).columns)))
        for seed in range(10)
    }

    assert len(structures) == 1
```

[#09](nb09.md) では木の形が実行のたびに変わりました。
**制限を掛けると再現性も上がります。**

## CGPA だけで判定が反転する

原著は 2 人の出願者を予測して見せます。**違いは CGPA だけ** です。

```python
def test_CGPAが高い出願者は合格と予測される(data, smaller) -> None:
    # 原著の出力: dt_smaller.predict([[320, 110, 3, 4.0, 3.5, 8.9, 0]]) -> array([ True])
    assert predict_applicant(smaller, data, [320, 110, 3, 4.0, 3.5, 8.9, 0]) is True


def test_CGPAだけ下げると不合格に変わる(data, smaller) -> None:
    # 原著の出力: 8.9 を 8.0 にすると array([False])
    # 他の 6 項目は同じ。根が CGPA なので、ここだけで判定が反転する
    assert predict_applicant(smaller, data, [320, 110, 3, 4.0, 3.5, 8.0, 0]) is False
```

根が CGPA の分割だからです。

```python
def test_小さい木の根はCGPAで分割する(data, smaller) -> None:
    # 7 つの特徴量のうち、成績（CGPA）がもっとも効く
    conditions = split_conditions(smaller, list(features(data).columns))

    assert conditions[0][0] == "CGPA"
```

**GRE も TOEFL も推薦状の評価も同じなのに、成績だけで結論が変わる。**
決定木が「読める」モデルだと言われる理由がここに出ています。

原著は 2 つ目のセルに `# A node in the white (neutral) leaf gets a false prediction`
とコメントしています。**その葉は合否が同数** で、
scikit-learn は同数のとき先頭のクラス（False）を選びます。
この規則は [F# 版](../fsharp/nb11.md) で自前実装するときに効いてきます。

## 2 特徴量にすると過学習が数字で見える

原著は GRE と TOEFL だけを使い、深さを変えて 3 枚の図を見せます。
図の代わりに正解率を並べました。

```python
def test_2特徴量の木は深さを増やすほど訓練データに当たる(data) -> None:
    # 原著は図でしか見せていないが、過学習の進み方が数値で追える
    scores = {
        depth: fit_exams(data, depth).score(data[EXAM_FEATURES], labels(data))
        for depth in [1, 2, None]
    }

    assert scores[1] == pytest.approx(0.8525)
    assert scores[2] == pytest.approx(0.8625)
    assert scores[None] == pytest.approx(0.93)
```

| 深さ | 節の数 | 正解率 |
| :--- | ---: | ---: |
| 1 | 3 | 0.8525 |
| 2 | 7 | 0.8625 |
| 制限なし | 197 | 0.9300 |

**節が 28 倍になって、正解率は 7 ポイントしか上がりません。**
しかも上がったぶんはほぼ確実に過学習です。

```python
def test_制限なしの木は節が増えても正解率は上がりきらない(data) -> None:
    # 深さ 2 の 7 節から制限なしの 197 節へ。節が 28 倍になっても
    # 正解率は 0.8625 から 0.93 にしか上がらない
    shallow = fit_exams(data, 2)
    unbounded = fit_exams(data, None)

    assert shallow.tree_.node_count == 7
    assert unbounded.tree_.node_count > 150
```

7 特徴量では 1.0 に届いたのに、2 特徴量では 0.93 で止まります。

```python
def test_2特徴量では制限なしでも1_0にならない(data) -> None:
    # 7 特徴量なら 1.0 になったが、2 特徴量では同じ点で違うラベルの出願者がいて
    # 完全には分けられない
    unbounded = fit_exams(data, None)

    assert unbounded.score(data[EXAM_FEATURES], labels(data)) < 1.0
```

**同じ GRE・TOEFL で合否が違う出願者がいる** からです。
決定木は同じ入力に違う答えを返せないので、ここが限界になります。

## まとめ

| 項目 | 原著と一致するか |
| :--- | :--- |
| データの規模（400 件・7 特徴量・180 合格） | 完全一致 |
| 制限なしの木の正解率（1.0） | 完全一致 |
| 最初の 5 件の予測 | 完全一致 |
| 小さい木の正解率（0.885）と節の数（15） | 完全一致 |
| 2 人の出願者の合否（True / False） | 完全一致 |

[Kotlin 版](../kotlin/nb11.md) では Smile の深さ指定が対応せず、
[F# 版](../fsharp/nb11.md) では自前 CART が **原著の数値をすべて再現** しました。

## 参照

- [ライブラリ版 Python 版 目次](index.md)
- [ライブラリ版 Kotlin 版 #11](../kotlin/nb11.md)
- [ライブラリ版 F# 版 #11](../fsharp/nb11.md)
- [自前実装版 第 9 章 決定木](../../python/ch09.md)
- [原著のノートブック](https://github.com/luisguiserrano/manning/blob/master/Chapter_09_Decision_Trees/University_Admissions.ipynb)
