# #12 回帰木（Python）

原著ノートブック: `Chapter_09_Decision_Trees/Regression_decision_tree.ipynb`

決定木を **回帰** に使う回です。年齢からアプリの利用日数を予測します。

分類木がジニ不純度を最小にする分割を探したのに対し、回帰木は
**平均二乗誤差（MSE）を最小にする分割** を探します。
原著は探索の過程を手で書き下し、9 通りの分割位置それぞれの重み付き MSE を並べます。

## サンプル実装

| 項目 | 内容 |
| :--- | :--- |
| 実装 | `apps/grokking-ml-python-lib/src/grokking_ml_lib/nb12_regression_tree.py` |
| テスト | `apps/grokking-ml-python-lib/tests/test_nb12_regression_tree.py` |
| 実行 | `uv run pytest tests/test_nb12_regression_tree.py` |

## データは 8 点だけ

```python
#: 原著が使う 8 点。年齢と、週あたりの利用日数
FEATURES = [[10], [20], [30], [40], [50], [60], [70], [80]]
LABELS = [7, 5, 7, 1, 2, 1, 5, 4]
```

年齢は 10 歳刻み、利用日数は 1〜7 日。
**若者と高齢者がよく使い、中年層が使わない** という U 字の形をしています。
線形回帰では捉えられない形なので、木を使う動機になります。

## 分割の探索を手で書く

原著は `range(0, 9)` で 9 通りの分割位置を回します。

```python
def split_mses(labels: list[int] = LABELS) -> list[SplitMse]:
    """分割位置を 0 から n まで動かし、それぞれの重み付き MSE を求める。

    原著は `range(0, 9)` と、要素数 8 に対して **9 通り** 回している。
    先頭（左が空）と末尾（右が空）の両方が含まれるので、
    分類木の回（[#08][nb08]）が `range(len(elements))` だったのと 1 つ違う。

    重みは「左右それぞれの二乗誤差の合計を、全体の件数で割る」形。
    各群の平均で割らないので、件数の多い群ほど強く効く。
    """
```

[#08](nb08.md) は `range(len(elements))` で **8 通り** でした。
同じ著者の同じ章で、**数え方が 1 つ違います**。
どちらでも最適な分割は変わりませんが、出力の行数が変わります。

```python
def test_分割は9通り試される() -> None:
    # 要素は 8 個だが range(0, 9) なので 9 通り。
    # 左が空の場合と右が空の場合の両方が含まれる
    splits = split_mses()

    assert len(splits) == 9
    assert splits[0].left == []
    assert splits[8].right == []
```

原著の 9 行の出力をすべて期待値にしました。

| 分割 | 左 \| 右 | 左の平均 | 右の平均 | 重み付き MSE |
| ---: | :--- | ---: | ---: | ---: |
| 0 | `[]` \| 全部 | NaN | 4.0 | 5.250000 |
| 1 | `7` \| 残り | 7.000 | 3.571 | 3.964286 |
| 2 | `7 5` \| 残り | 6.000 | 3.333 | 3.916667 |
| **3** | `7 5 7` \| 残り | 6.333 | 2.600 | **1.983333** |
| 4 | `7 5 7 1` \| 残り | 5.000 | 3.000 | 4.250000 |
| 5 | ... | 4.400 | 3.333 | 4.983333 |
| 6 | ... | 3.833 | 4.500 | 5.166667 |
| 7 | ... | 4.000 | 4.000 | 5.250000 |
| 8 | 全部 \| `[]` | 4.000 | NaN | 5.250000 |

**分割 3 が飛び抜けて良い** ことが数字で分かります。
「よく使う若者 3 人」と「それ以外 5 人」に分ける位置です。

## 空の側は NaN になる

```python
def test_左が空のときの平均はNaNになる() -> None:
    # 原著も NumPy の RuntimeWarning つきで nan を出している
    first = split_mses()[0]

    assert math.isnan(first.left_mean)
    assert first.right_mean == pytest.approx(4.0)
```

原著の実行結果には NumPy の警告が残っています。

```text
RuntimeWarning: Mean of empty slice.
RuntimeWarning: invalid value encountered in double_scalars
```

**平均は NaN ですが、MSE は 5.25 と正しく出ます。**
空の配列との内積が 0 になるので、NaN が伝播しないためです。
[#08](nb08.md) でジニ不純度が空のリストで壊れなかったのと同じ構造です。

## scikit-learn も同じ位置で分割する

```python
def test_scikit_learnも同じ位置で分割する() -> None:
    # 手計算の最小値は 3 番目、つまり 30 歳と 40 歳の間。中点の 35.0 が選ばれる
    conditions = split_conditions(fit())

    assert conditions[0] == pytest.approx(35.0)
```

手計算の最小値（分割 3）は 30 歳と 40 歳の間です。
`DecisionTreeRegressor` はその中点 **35.0** を選びます。

```python
def test_深さ2の木は3回分割する() -> None:
    # 根で 35.0、左で 15.0、右で 65.0
    assert split_conditions(fit()) == pytest.approx([35.0, 15.0, 65.0])
```

**原著が手で確かめたのは根の分割だけ** ですが、
深さ 2 の木は 3 回分割します。

## 葉は平均を返す

分類木の葉は多数決でした。回帰木の葉は **平均** を返します。

```python
def leaf_values(model: DecisionTreeRegressor) -> list[float]:
    """葉が返す予測値を、節の順に並べる。

    回帰木の葉は **その葉に落ちた点の平均** を返す。分類木が多数決だったのと違う。
    """
```

```python
def test_葉の値は元のラベルの平均になっている() -> None:
    # 40, 50, 60 歳のラベルは 1, 2, 1。その平均 4/3 が葉の値
    assert (1 + 2 + 1) / 3 == pytest.approx(4 / 3)
    assert leaf_values(fit())[2] == pytest.approx(4 / 3)
```

40・50・60 歳の利用日数は 1, 2, 1 日。その平均 **1.333 日** が予測になります。

## 予測は階段状になる

```python
def test_予測は階段状になる() -> None:
    # 同じ葉に落ちる点は同じ値を返す。回帰木の予測は連続にならない
    predictions = fit().predict(FEATURES).tolist()

    assert predictions == pytest.approx([7.0, 6.0, 6.0, 4 / 3, 4 / 3, 4 / 3, 4.5, 4.5])
```

| 年齢 | 予測 |
| ---: | ---: |
| 10 | 7.000 |
| 20, 30 | 6.000 |
| 40, 50, 60 | 1.333 |
| 70, 80 | 4.500 |

**4 段の階段** です。線形回帰なら直線、ロジスティック回帰ならなめらかな曲線でしたが、
回帰木の出力は **不連続** になります。

原著の `plot_regressor` が描いているのは、まさにこの階段です。
中間の年齢（たとえば 35 歳）を入れても、必ずこの 4 つの値のどれかが返ります。

## まとめ

| 項目 | 原著と一致するか |
| :--- | :--- |
| 9 通りの重み付き MSE | 完全一致（NaN の扱いも含めて） |
| 根の分割（35.0） | 完全一致 |
| 木の分割（35.0 / 15.0 / 65.0） | 完全一致（原著は図でしか示していない） |
| 葉の予測値 | 完全一致 |

Kotlin も F# も同じ結果になりました。
**分類木で 3 本続いた食い違いが、回帰木では起きません。**
回帰の分割基準は最小二乗しかなく、同点も起きにくいためです。

## 参照

- [ライブラリ版 Python 版 目次](index.md)
- [ライブラリ版 Kotlin 版 #12](../kotlin/nb12.md)
- [ライブラリ版 F# 版 #12](../fsharp/nb12.md)
- [自前実装版 第 9 章 決定木](../../python/ch09.md)
- [原著のノートブック](https://github.com/luisguiserrano/manning/blob/master/Chapter_09_Decision_Trees/Regression_decision_tree.ipynb)
